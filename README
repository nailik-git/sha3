# sha3
this is a library for computing sha3-hashes, for usage look into [the example](example.c)

this library is NOT TESTED, i have however checked some hashes with existing implementations

## quickstart
```bash
$ make
$ build/example
```
